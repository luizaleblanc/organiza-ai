import kof.ui
import screens.budget_models.screen

main() {
    var window = Window("Organiza IA · Modelos de orçamento")
    window.theme = Theme.dark()
    Router.route("budget-models", buildBudgetModelsScreen())
    Router.go("budget-models")
    window.show()
}
